//import { instance } from "./api";
//import { ProductType } from "../@types/product";

export default {
	getAllProducts: () => {
		return
	},
	getProductById: (
		id: number,
	) => {
		return id;
	},
	getProductByName: (
		productName: string,
	) => {
		return productName;
	}
}